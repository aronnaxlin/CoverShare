{
  "name": "covershare-api",
  "version": "1.0.0",
  "description": "API service for CoverShare iOS Shortcuts integration",
  "main": "api/generate.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "vercel": "^33.0.0"
  }
}
